#!/bin/bash
set -e

# Stop and disable service before removing files
if command -v systemctl &>/dev/null; then
    systemctl stop armor-pilot.service 2>/dev/null || true
    systemctl disable armor-pilot.service 2>/dev/null || true
fi
